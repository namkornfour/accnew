version: '3.9'
services:
  frontend:
    #image: acctax
    #volumes:
    #  - .:/app
    #  - /app/node_modules
    #ports:
    #  - "3000:3000"
    #command: npm start

    image: acctax
    container_name: acctax
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - '.:/app'
      - '/app/node_modules'
    ports:
      - 3001:3000
    environment:
      - CHOKIDAR_USEPOLLING=true
    stdin_open: true